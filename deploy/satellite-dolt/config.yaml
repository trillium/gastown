# Satellite Dolt server configuration
# SQL on 3307, remotesapi on 8000

log_level: warning

listener:
  host: 0.0.0.0
  port: 3307
  max_connections: 1000

behavior:
  dolt_transaction_commit: true

remotesapi:
  port: 8000
  read_only: false
